<!DOCTYPE html>
<html>
<head lang="pt">
    <meta charset="UTF-8">
    <title>Doctrine</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>{% block local %}Produtos Cadastrados no Sistema{% endblock %}</h1>

            <form action="{{ path('pesquisar') }}" method="post">
                <div class="form-group">
                    <div class="col-sm-6 col-sm-offset-2">
                        <input type="text" class="form-control" id="nome" name="nome" placeholder="Digite o nome do produto aqui" required="required" required>
                    </div>
                    <div class="col-sm-3">
                        <a href=""><button type="submit" class="btn btn-info" >Pesquisar</button></a>
                    </div>
                </div>
            </form>

            {% block content %}
            {% endblock %}
            <footer>
                <p>Doctrine | &copy Todos os direitos reservados</p>
            </footer>
        </div><!-- /col-md-12 -->
    </div><!-- /row -->
</div><!-- /container-fluid -->
</body>
</html>